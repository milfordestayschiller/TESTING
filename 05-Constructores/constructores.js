class CalculadoraNPI  {
   constructor(calculadora) {
       this.calculadora = calculadora;
   }
   agregar(num1) {
       
       return num1
   }

  sumar(valor) {
   
       return valor
   }
   
valor (valor1,valor2) {
return `${valor1+valor2}`
}

}

var calculadora = new CalculadoraNPI("Calculadora que use notación polaca inversa")

